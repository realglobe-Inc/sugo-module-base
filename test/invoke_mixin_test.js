/**
 * Test case for invokeMixin.
 * Runs with mocha.
 */
'use strict'

const invokeMixin = require('../lib/mixins/invoke_mixin.js')
const assert = require('assert')
const co = require('co')

describe('invoke-mixin', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Invoke mixin', () => co(function * () {

  }))
})

/* global describe, before, after, it */
